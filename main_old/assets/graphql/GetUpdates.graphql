query GetUpdates($incidentId: Int!) {
    getUpdatesForIncidentId(id: $incidentId) {
        id
        at
        title
        incident {
            id
        }
        user {
            id
        }
    }
}